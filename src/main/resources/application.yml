spring:
  application:
    name: parking

  datasource:
    url: jdbc:postgresql://localhost:6969/parkingdb
    driver-class-name: org.postgresql.Driver
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

